# Simple To-Do List Application

This is a simple 3-tier To-Do list application built with Node.js, Express, and React. It demonstrates a basic 3-tier architecture.

## Installation

1. Clone the repository.
2. Install dependencies using `npm install`.
3. Start the application using `npm start`.

## Structure

- **Presentation Layer**: React components.
- **Business Logic Layer**: Express controllers and services.
- **Data Access Layer**: In-memory data store.
